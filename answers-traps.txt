### Exercise 1

1. a0～a7用来作为函数参数，main函数中 13 存在了寄存器 a2 中。

2. 并没有调用f和g，而是直接将代入计算后的结果作为了寄存器a1的值(即12)传入了 printf 中

3. 0x630

4. ra存的是printf执行完后跳转回到main的地址，因此ra的值为 0x38

5. 57616 转换为 16 进制为 0xe110，因此前半部分输出为 he110，后半部分会将0x00646c72根据ascii码转换为字符串，由于为小端法，因此将 72,6c,64 依次对照 ASCII 表，分别得到 r,l,d，因此输出结果为 He110 World 

   若改用大端法，则 i=0x726c64。

   57616的值不需要改变，因为其转换成16进制是不变的。

6. y的值应为寄存器a2的值，因为printf会从a2寄存器中读取参数作为y的值。